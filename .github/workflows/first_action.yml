name: First workflow

on: workflow_dispatch

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "hello 1"

      - name: Print bye
        run: echo "hello 2"
